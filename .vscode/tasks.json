{
  "version": "2.0.0",
  "tasks": [
    {
        "label": "Run in .venv",
        "type": "shell",
        "command": "${workspaceFolder}/.venv/bin/python ${file}",
        "problemMatcher": [
            "$python"
        ]
    }
]
}